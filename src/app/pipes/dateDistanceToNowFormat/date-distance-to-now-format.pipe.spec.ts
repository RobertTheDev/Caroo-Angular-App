import { DateDistanceToNowFormatPipe } from './date-distance-to-now-format.pipe';

describe('DateDistanceToNowFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDistanceToNowFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
